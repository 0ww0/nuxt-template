// End-user area, auto-registered as a layer. Priority `3.` is highest among the
// layers; it owns the primary /login route that the auth/role middleware
// redirect to by default.
export default defineNuxtConfig({})
